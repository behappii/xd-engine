/target
.DS_Store
# Картинки не в репозитории: они большие и у каждого свои. Каталог при этом
# нужен — git пустых каталогов не хранит, поэтому держим его файлом-якорем
/textures/*
!/textures/.gitkeep
